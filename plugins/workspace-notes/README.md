# `@dsh-lite/plugin-workspace-notes`

Registers `lite_notes` for a bounded `.dsh-lite/notes.md`. `lstat` and `realpath` checks reject absolute escapes, traversal, symlinks, and parent-directory symlink escapes.
