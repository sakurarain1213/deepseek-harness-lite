# `@dsh-lite/plugin-session-export`

Registers `lite_session_export`. Projection is allowlist-only: supported user, assistant, tool, and turn event types contribute only explicitly selected fields to JSON or Markdown.
