# `@dsh-lite/plugin-safe-fetch`

Registers `lite_safe_fetch`. Every request target and redirect is resolved and rejected when any address is non-public. Protocol, credentials, redirects, response bytes, and elapsed time are bounded.
